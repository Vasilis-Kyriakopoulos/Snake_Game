# AGENTS.md

## Project Notes
- This is a small app project in the repository root.
- Keep changes minimal and focused on the user's request.

## Conventions
- Prefer simple HTML, CSS, and JavaScript changes.
- Avoid adding dependencies unless the user asks for them.
- Preserve existing file structure unless a restructure is necessary.
- Ask the user for confirmation before running any `git add`, `git commit`, or `git push` command.

## Verification
- When making UI changes, verify that `index.html`, `styles.css`, and `app.js` remain consistent.
